# 参与开发

需要 Node.js 22+。Fork 后安装并验证：

```bash
npm ci
npx playwright install chromium
npm run verify:all
```

开发工作台：`npm run studio`，访问终端打印的本机地址。离线模式无需密钥；不得把固定样例标为真实模型结果。

提交 PR 请说明问题、行为变化、验证结果与未验证边界。涉及平台写入的修改，应覆盖重试、输入变化、人工修改和撤销；涉及 UI，补充用样例数据拍摄的截图。新增配置须说明默认值与数据去向。

不提交 `.env`、`*.local.json`、任务历史、浏览器配置、真实反馈和生成目录。发布素材仅使用自行创建的样例。运行 `npm run package` 验证 ZIP 与校验和，分发包须保留许可证。

本仓库当前不接受把真实写入伪装成只读、静默回退到假模型结果、自动上传用户任务历史的改动。发现缺陷请使用 Issue 模板；不要公开访问密钥或可利用的安全细节。
